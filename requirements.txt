# Railway will use this file if nixpacks.toml is not present
# For Railway/Nixpacks: the nixpacks.toml file handles installation
# This file lists the backend dependencies explicitly
flask>=3.0.0
celery>=5.3.0
redis>=5.0.0
gunicorn>=21.2.0

