[build]
builder = "NIXPACKS"
buildCommand = "pip install . && pip install flask>=3.0.0 celery>=5.3.0 redis>=5.0.0 && pip install -U babeldoc<0.3.0 pymupdf<1.25.3 pdfminer-six==20250416"

[deploy]
startCommand = "pdf2zh --celery worker --loglevel=info"
healthcheckPath = ""
healthcheckTimeout = 100

